export type Options = {
    slidesPerView?: Object,
    swipeOnSlide?: boolean,
    speedStory?: number,
    speedAnimNextSlide?: number,
    navigation?: {
        elBtnPrev: string,
        elBtnNext: string,
    },
}