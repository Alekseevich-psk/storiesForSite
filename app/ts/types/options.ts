export type Options = {
    slidesPerView?: Object,
    swipeOnSlide?: boolean,
    autoPlayFullScreen: boolean,
    storiesMode: boolean,
    sliderMode: boolean,
    speedStory?: number,
    speedAnimNextSlide?: number,
    navigation?: {
        elBtnPrev: string,
        elBtnNext: string,
    },
}